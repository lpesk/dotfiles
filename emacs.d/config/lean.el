(require 'lean-mode)
(require 'company-lean)

(setq lean-message-boxes-enabled-captions '("check result", "eval
result" "print result" "reduce result" "trace output"))
(setq lean-message-boxes-enabledp t)
